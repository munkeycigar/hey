## Coding Agent — Add Docker app image
**Files changed:** `.dockerignore`, `Dockerfile`, `docker/entrypoint.sh`, `tests/test_docker_artifacts.py`, `ARCHITECTURE.md`, `PLAN.md`, `HANDOFF.md`, `docs/README.md`, `docs/architecture.md`, `docs/features/deployment.md`, `docs/CHANGELOG.md`
**Docs updated:** `docs/README.md`, `docs/architecture.md`, `docs/features/deployment.md`, `docs/CHANGELOG.md`
**What was done:** Added the Django app Docker image path with build context exclusions and an executable entrypoint that runs migrations, collectstatic, and Gunicorn. Added a dependency-free artifact test and scoped docs for the Docker image.
**For next agent:** Run `docker build -t hey-app:test .` and a container startup check in an environment with Docker daemon access; this sandbox blocks `/Users/leoreyes/.docker/run/docker.sock` with `connect: operation not permitted`. Commit the changes in an environment that can write `.git/index.lock`; this sandbox returns `Operation not permitted` for `.git` writes. Compose and Postgres service changes are intentionally left for the next deployment task.

## Coding Agent — Replace Compose With App And Postgres Deployment
**Files changed:** `.env.example`, `ARCHITECTURE.md`, `PLAN.md`, `docker-compose.yml`, `docs/CHANGELOG.md`, `docs/README.md`, `docs/architecture.md`, `docs/features/deployment.md`, `tests/test_docker_artifacts.py`, `HANDOFF.md`
**Docs updated:** `docs/CHANGELOG.md`, `docs/README.md`, `docs/architecture.md`, `docs/features/deployment.md`
**What was done:** Replaced the generated app-only Compose file with `db` and `app` services, a Postgres healthcheck, `127.0.0.1:9600:8000` app publishing, deployment environment defaults, and the named `postgres_data` volume. Updated `.env.example`, docs, architecture notes, and the Docker artifact test to cover the app plus Postgres deployment contract.
**For next agent:** `python3 -m unittest discover -s tests -v`, `docker compose config --services`, `docker compose config --volumes`, and `docker compose config --no-interpolate` pass in this sandbox. `docker compose up -d --build` still needs to be run in a Docker-enabled environment because this sandbox cannot access `/Users/leoreyes/.docker/run/docker.sock` (`connect: operation not permitted`). Commit also needs to be retried in an environment that can write `.git/index.lock`; `git add` returns `Operation not permitted` here. Compose renders the loopback port in canonical form as `host_ip: 127.0.0.1` plus `published: "9600"` rather than as a compact `127.0.0.1:9600` string.

## Coding Agent — Document Localhost Deployment
**Files changed:** `README.md`, `docs/README.md`, `docs/architecture.md`, `docs/features/deployment.md`, `docs/CHANGELOG.md`, `ARCHITECTURE.md`, `PLAN.md`, `HANDOFF.md`
**Docs updated:** `docs/README.md`, `docs/architecture.md`, `docs/features/deployment.md`, `docs/CHANGELOG.md`
**What was done:** Added human-facing documentation for deploying Hey on `127.0.0.1:9600` behind an existing external Cloudflare Tunnel. The docs now cover start, verify, logs, update, and stop commands plus the required tunnel origin `http://127.0.0.1:9600`.
**For next agent:** Fresh verification in this sandbox: `rg -n "Cloudflare Tunnel|http://127\\.0\\.0\\.1:9600|docker compose up -d --build|docker compose logs -f app|git pull|docker compose down" README.md docs/README.md docs/architecture.md docs/features/deployment.md docs/CHANGELOG.md ARCHITECTURE.md` found the required documentation coverage; `git diff --check` passed; `python3 -m unittest discover -s tests -v` passed with 2 tests. No runtime code or API surface changed. Commit still needs to be retried in an environment that can write `.git/index.lock`; this sandbox returns `Operation not permitted` during `git add`.
